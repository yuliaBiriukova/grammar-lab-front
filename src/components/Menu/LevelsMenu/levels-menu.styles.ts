import {theme} from "../../../utils/theme";

export const levelsMenuStyles = {
    container: {
        backgroundColor: theme.palette.common.white,
        borderRadius: '8px',
        padding: '24px',
        width: 232,
        boxShadow: '4px 2px 12px 0 rgba(0, 0, 0, 0.06), 0 0 8px 0 rgba(0, 0, 0, 0.04)',
        marginRight: '48px'
    },
    linkActive: {
        color: theme.palette.primary.main,
    }
}